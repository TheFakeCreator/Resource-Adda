name: CI/CD Pipeline

on:
  push:
    branches: ["2.0", "master"]
  pull_request:
    branches: ["2.0", "master"]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
          run_install: false
      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm install
      - name: Check Format
        run: pnpm run format:check

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
          run_install: false
      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm install
      - name: Lint
        run: pnpm run lint

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
          run_install: false
      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm install
      - name: Build Frontend
        run: pnpm --filter frontend run build

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10
          run_install: false
      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm install
      - name: Test Backend
        run: pnpm --filter backend run test
# name: CI/CD Pipeline

# on:
#   push:
#     branches: ["2.0", "master"]
#   pull_request:
#     branches: ["2.0", "master"]

# # Forces actions to use Node 24, preventing the deprecation warnings
# env:
#   FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# jobs:
#   format:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Install pnpm
#         uses: pnpm/action-setup@v4
#         # 'version' removed so it automatically matches package.json

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           cache: "pnpm" # This replaces the entire manual cache setup

#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

#       - name: Check Format
#         run: pnpm run format:check

#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install pnpm
#         uses: pnpm/action-setup@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           cache: "pnpm"
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile
#       - name: Lint
#         run: pnpm run lint

#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install pnpm
#         uses: pnpm/action-setup@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           cache: "pnpm"
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile
#       - name: Build Frontend
#         run: pnpm --filter frontend run build

#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install pnpm
#         uses: pnpm/action-setup@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           cache: "pnpm"
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile
#       - name: Test Backend
#         run: pnpm --filter backend run test
